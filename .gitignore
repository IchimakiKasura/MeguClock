.vscode

# legacy ones
# IchiClock (Legacy)
# rtc (Legacy).h

# bin folder is where the 'arduino-cli.exe' placed
bin/arduino-cli.exe

# symlink folders
MeguClock_DS3231 (symlink)
MeguClock_ST7735 (symlink)